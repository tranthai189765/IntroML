# ── Core ──────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
tqdm>=4.65
pyarrow>=12.0          # đọc/ghi parquet embeddings

# ── Embedding (trích xuất đặc trưng) ─────────────────────
torch>=2.0             # GPU khuyến nghị
torchvision>=0.15
transformers>=4.45     # SigLIP 2 (google/siglip2-so400m-patch16-384)
FlagEmbedding>=1.2     # BGE-M3 (BAAI/bge-m3)
Pillow>=10.0

# ── Adaptive Random Forest ───────────────────────────────
river>=0.21,<0.22      # ARFClassifier / ARFRegressor + ADWIN

# ── Thu thập dữ liệu (tùy chọn) ──────────────────────────
requests>=2.31
