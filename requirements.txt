# --- x_pipeline.py (data collection) ---
requests>=2.31,<3

# --- analyze_figures.py (data-quality figures/tables) ---
matplotlib>=3.7
numpy>=1.24

# stdlib used (no install needed): sqlite3, concurrent.futures, argparse, json, re, csv

# --- optional: image validation/resize before training ---
# Pillow>=10

# --- embed_dataset.py (text+image embeddings) ---
# torch>=2.2
# transformers>=4.45        # SigLIP2 image encoder
# FlagEmbedding>=1.2        # BGE-M3 dense text embedding
# pillow>=10
# pandas>=2.2
# pyarrow>=15               # parquet output

# --- optional: multimodal online-learning training side ---
# scikit-learn>=1.4         # SGDClassifier/Regressor (online) ; or lightgbm>=4 for best small-data accuracy
# lightgbm>=4.0
